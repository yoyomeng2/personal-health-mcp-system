# Production environment config
# Extends personal_health.yaml

server:
  host: "0.0.0.0"
  port: 8080
  debug: false

database:
  path: "data/health.prod.db"
  timeout: 30

logging:
  level: "WARNING"
